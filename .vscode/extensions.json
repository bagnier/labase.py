{
  "recommendations": [
    // Python
    "ms-python.python",

    // Ruff — linting + formatting (remplace black, isort, flake8)
    "charliermarsh.ruff",

    // ty — type checking (Astral)
    "astral-sh.ty",

    // Jinja2 templates
    "samuelcolvin.jinjahtml",

    // Tailwind CSS (autocomplétion dans les classes HTML)
    "bradlc.vscode-tailwindcss",

    // HTMX (autocomplétion des attributs hx-*)
    "otovo-oss.htmx-tags",

    // Docker
    "ms-azuretools.vscode-containers",

    // Coverage dans le gutter (lignes couvertes/non couvertes)
    "ryanluker.vscode-coverage-gutters",

    // .env
    "mikestead.dotenv",

    // Gherkin / pytest-bdd
    "alexkrechik.cucumberautocomplete",

    // TOML (pyproject.toml, uv.lock)
    "tamasfe.even-better-toml",

    // SQL — sqlfluff linting (config: scripts/.sqlfluff)
    "dorzey.vscode-sqlfluff",

    // Biome — JS / CSS / JSON linting + formatting
    "biomejs.biome",

    // GitHub Actions — workflow validation + autocomplete
    "github.vscode-github-actions"
  ]
}
