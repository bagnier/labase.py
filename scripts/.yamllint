# yamllint config — see README "Quality tools".
extends: default

rules:
  line-length:
    max: 120
  document-start: disable   # the `---` prefix is not required
  truthy:
    check-keys: false        # allow the GitHub Actions `on:` key
